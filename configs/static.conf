# Module 5 test config. Each non-"/" location has a root corresponding
# to where its content lives — because the Router strips the location
# prefix per the subject's /kapouet rule.

server {
    listen 127.0.0.1:8080;
    server_name static-test;
    client_max_body_size 1m;

    error_page 404 /errors/404.html;

    location / {
        root /tmp/webserv_www;
        index index.html;
        methods GET;
    }

    location /static {
        root /tmp/webserv_www/static;
        methods GET;
    }

    location /files {
        root /tmp/webserv_www/files;
        methods GET;
        autoindex on;
    }

    location /deletable {
        root /tmp/webserv_www/deletable;
        methods GET DELETE;
    }

    location /old {
        return 301 /;
    }

    location /errors {
        root /tmp/webserv_www/errors;
        methods GET;
    }
}
