# Module 7 test config: file upload endpoint.

server {
    listen 127.0.0.1:8080;
    server_name upload-test;
    client_max_body_size 10m;

    # Static root
    location / {
        root /tmp/webserv_www;
        index index.html;
        methods GET;
    }

    # Upload endpoint
    location /upload {
        root /tmp/webserv_uploads;
        methods POST GET DELETE;
        upload_store /tmp/webserv_uploads;
        autoindex on;
    }

    # Per-location small limit, for testing 413
    location /small-upload {
        root /tmp/webserv_uploads;
        methods POST;
        upload_store /tmp/webserv_uploads;
        client_max_body_size 100;
    }
}
